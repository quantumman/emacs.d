;;; less is more
(tool-bar-mode -1)
(menu-bar-mode -1)
(scroll-bar-mode -1)

;;; show testing on the frame title
(setq frame-title-format "emacs-test")

;;; run debugger if error occurred
(setq debug-on-error)


